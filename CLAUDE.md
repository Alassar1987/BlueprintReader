# BlueprintReader — Claude Code entry point

Единый источник истины для ИИ-агентов — приватная папка `.docs/` (не в git).

Прочитай сначала `.docs/AGENTS.md`, затем остальные файлы `.docs/` по мере необходимости (vision, roadmap, architecture).

Git commit/push — только по прямому запросу пользователя.
