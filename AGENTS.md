# BlueprintReader — AI Entry Point

Инструкции для ИИ-агентов находятся **только** в приватной папке `.docs/` (не клонируется в git).

Начни с:

```
.docs/AGENTS.md
```

Затем читай `.docs/vision.md`, `.docs/roadmap.md`, `.docs/architecture.md` по мере необходимости.
